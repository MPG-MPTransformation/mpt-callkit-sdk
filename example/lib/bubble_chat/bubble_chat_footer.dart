import 'package:flutter/material.dart';

class BubbleChatFooter extends StatelessWidget {
  const BubbleChatFooter({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
      decoration: const BoxDecoration(
        color: Colors.grey,
      ),
    );
  }
}
